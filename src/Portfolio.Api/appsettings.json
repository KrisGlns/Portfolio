{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Cors": {
    "// AllowedOrigins": "Empty means any origin. Set this to your published site URL before going live.",
    "AllowedOrigins": []
  },
  "Resend": {
    "// ApiKey": "NEVER commit this. Set the environment variable Resend__ApiKey on the host.",
    "ApiKey": "",
    "// From": "Resend's sandbox sender needs no verified domain, but may only deliver to the account's own address.",
    "From": "Portfolio <onboarding@resend.dev>",
    "To": "chris_glns@hotmail.com",
    "SubjectPrefix": "Portfolio"
  }
}
